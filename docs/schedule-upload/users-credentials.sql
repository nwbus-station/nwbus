-- ============================================================
--  NWBUS — بيانات دخول الموظفين + دور مشرف الوردية
--  شغّل هذا مرة واحدة في: Supabase Dashboard → SQL Editor
-- ============================================================

-- 1. عمود كلمة المرور (للأدمن فقط — لا يظهر للمستخدمين)
ALTER TABLE users
  ADD COLUMN IF NOT EXISTS login_password text;

-- 2. رقم الهوية / الإقامة
ALTER TABLE users
  ADD COLUMN IF NOT EXISTS national_id text;

-- 2. سياسة RLS: الأدمن فقط يقرأ/يكتب login_password
-- (إن كان RLS مفعّلاً على جدول users)
-- لا تحتاج تغيير إضافي إذا كان الأدمن لديه وصول كامل للجدول.

-- ملاحظة: دور shift_supervisor يُضبط في الكود فقط (قيمة نصية في عمود role).
-- لا تغيير في قاعدة البيانات مطلوب لإضافة الدور.
